
src\widgets has code which is goes into the plugin
widget DLLs for the XCenter. Each C code file will
be in a separate DLL, which will end up in the
plugins\xcenter directory of the XWorkplace installation
directory.

See src\shared\center.c for an introduction to the XCenter.

Ulrich M”ller (2000-12-07)


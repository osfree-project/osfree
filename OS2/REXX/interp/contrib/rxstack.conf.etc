# This file; /etc/rxstack.conf enables rxstack running as a system daemon to
# be configured by changing a config file
#
# port: the TCP/IP post the rxstack process listens on; 5757 by default
#port=5757
#
# world: if set to no, the default, only connections from localhost are accepted
# setting world = yes allows connections from any machine on any network connection
# use yes with care!
#world=no

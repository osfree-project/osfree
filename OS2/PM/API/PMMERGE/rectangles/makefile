#
# A Makefile for pmmerge.dll
# (c) osFree project,
# author, date
#

srcfiles = $(p)rectangles$(e)
ADD_COPT = -i=$(MYDIR)..$(SEP)include -bd
DEST =

!include $(%ROOT)/mk/appsos2.mk

TARGETS = $(OBJS)

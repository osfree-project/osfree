#
# A Makefile for pmmerge.dll
# (c) osFree project,
# author, date
#

srcfiles = $(p)messages$(e) $(p)queue$(e)
ADD_COPT = -i=$(MYDIR).. -bd
DEST = none

!include $(%ROOT)/mk/appsos2.mk

TARGETS = $(OBJS)

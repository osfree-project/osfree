#
# A Makefile for pmmerge.dll
# (c) osFree project,
# author, date
#

srcfiles = $(p)messages$(e) $(p)queue$(e)
ADD_COPT = -i=$(MYDIR).. -bd

!include $(%ROOT)/mk/appsos2_fpm.mk

DEST = none
TARGETS = $(OBJS)

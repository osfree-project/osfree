group stddef;

type ptrdiff_t=int;

type size_t=UINT;

type wchar_t=USHORT;

(* type NULL=nil; *)

(* #define offsetof(s_name, s_member) (size_t)&(((s_name * )0)->s_member) *)

(* Also here place to define wchar_t. Most probably as separate file because used in
  wchar.h (in C) also. *)

end.
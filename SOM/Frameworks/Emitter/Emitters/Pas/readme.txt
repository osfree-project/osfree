SOM COMPILER PASCAL EMITTER README FILE
=======================================

SOM Compiler Pascal Emitter is a extension of SOM Compiler to support
usage of Pascal for SOM Objects access. At the present time it is
only supports client creation (usage), not server (implementation).


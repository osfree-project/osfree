
DEST = os2tk45$(SEP)som$(SEP)include

!include $(%ROOT)SOM/common.mk

TARGETS = $(PATH)somobj.idl &
                $(PATH)somcls.idl &
                $(PATH)somcm.idl &
                $(PATH)attribdf.idl &
                $(PATH)constdef.idl &
                $(PATH)containd.idl &
                $(PATH)containr.idl &
                $(PATH)excptdef.idl &
                $(PATH)intfacdf.idl &
                $(PATH)moduledf.idl &
                $(PATH)operatdf.idl &
                $(PATH)paramdef.idl &
                $(PATH)repostry.idl &
                $(PATH)typedef.idl &
                $(PATH)boa.idl &
                $(PATH)cntxt.idl &
                $(PATH)impldef.idl &
                $(PATH)implrep.idl &
                $(PATH)nvlist.idl &
                $(PATH)om.idl &
                $(PATH)orb.idl &
                $(PATH)poa.idl &
                $(PATH)principl.idl &
                $(PATH)request.idl &
                $(PATH)rhborbst.idl &
                $(PATH)rhbtypes.idl &
                $(PATH)servmgr.idl &
                $(PATH)servreq.idl &
                $(PATH)somdcprx.idl &
                $(PATH)somdfenc.idl &
                $(PATH)somdmprx.idl &
                $(PATH)somdobj.idl &
                $(PATH)somdom.idl &
                $(PATH)somdserv.idl &
                $(PATH)somdstub.idl &
                $(PATH)somdtcdt.idl &
                $(PATH)somdtype.idl &
                $(PATH)somoa.idl &
                $(PATH)stexcep.idl &
                $(PATH)unotypes.idl &
                $(PATH)clientev.idl &
                $(PATH)eman.idl &
                $(PATH)emregdat.idl &
                $(PATH)event.idl &
                $(PATH)sinkev.idl &
                $(PATH)timerev.idl &
                $(PATH)workprev.idl &
                $(PATH)defedidl.idl &
                $(PATH)eda.idl &
                $(PATH)fma.idl &
                $(PATH)fmi.idl &
                $(PATH)fmib.idl &
                $(PATH)fsagm.idl &
                $(PATH)fsgm.idl &
                $(PATH)iogma.idl &
                $(PATH)iogrp.idl &
                $(PATH)mia.idl &
                $(PATH)nsma.idl &
                $(PATH)objset.idl &
                $(PATH)pid.idl &
                $(PATH)po.idl &
                $(PATH)poid.idl &
                $(PATH)poida.idl &
                $(PATH)psma.idl &
                $(PATH)somp.idl &
                $(PATH)somperr.idl &
                $(PATH)somperrd.idl &
                $(PATH)sompstad.idl &
                $(PATH)sompstat.idl &
                $(PATH)srkset.idl &
                $(PATH)snglicls.idl &
                $(PATH)sombacls.idl &
                $(PATH)somida.idl &
                $(PATH)somproxy.idl &
                $(PATH)somsid.idl &
                $(PATH)scattrib.idl &
                $(PATH)scbase.idl &
                $(PATH)scclass.idl &
                $(PATH)sccommon.idl &
                $(PATH)scconst.idl &
                $(PATH)scdata.idl &
                $(PATH)scemit.idl &
                $(PATH)scentry.idl &
                $(PATH)scenum.idl &
                $(PATH)scenumnm.idl &
                $(PATH)scmeta.idl &
                $(PATH)scmethod.idl &
                $(PATH)scmodule.idl &
                $(PATH)scparm.idl &
                $(PATH)scpass.idl &
                $(PATH)scseqnce.idl &
                $(PATH)scstring.idl &
                $(PATH)scstruct.idl &
                $(PATH)sctdef.idl &
                $(PATH)sctmplt.idl &
                $(PATH)scunion.idl &
                $(PATH)scusrtyp.idl &
                $(PATH)somstrt.idl &
                $(PATH)somssock.idl &
                $(PATH)tcpsock.idl &
                $(PATH)omgestio.idl &
                $(PATH)somestio.idl &
                $(PATH)biter.idl &
                $(PATH)lname.idl &
                $(PATH)lnamec.idl &
                $(PATH)naming.idl &
                $(PATH)xnaming.idl &
                $(PATH)xnamingf.idl &
                $(PATH)omgidobj.idl &
                $(PATH)corba.idl &
                $(PATH)somref.idl &
                $(PATH)somap.idl &
                $(PATH)somos.idl &
                $(PATH)xmscssae.idl &
                $(PATH)somtdm.idl

SOURCES = $(PORT_BASE)somkpub$(SEP)som$(SEP)somobj.idl &
                $(PORT_BASE)somkpub$(SEP)som$(SEP)somcls.idl &
                $(PORT_BASE)somkpub$(SEP)som$(SEP)somcm.idl &
                $(PORT_BASE)somir$(SEP)attribdf.idl &
                $(PORT_BASE)somir$(SEP)constdef.idl &
                $(PORT_BASE)somir$(SEP)containd.idl &
                $(PORT_BASE)somir$(SEP)containr.idl &
                $(PORT_BASE)somir$(SEP)excptdef.idl &
                $(PORT_BASE)somir$(SEP)intfacdf.idl &
                $(PORT_BASE)somir$(SEP)moduledf.idl &
                $(PORT_BASE)somir$(SEP)operatdf.idl &
                $(PORT_BASE)somir$(SEP)paramdef.idl &
                $(PORT_BASE)somir$(SEP)repostry.idl &
                $(PORT_BASE)somir$(SEP)typedef.idl &
                $(PORT_BASE)somd$(SEP)boa.idl &
                $(PORT_BASE)somd$(SEP)cntxt.idl &
                $(PORT_BASE)somd$(SEP)impldef.idl &
                $(PORT_BASE)somd$(SEP)implrep.idl &
                $(PORT_BASE)somd$(SEP)nvlist.idl &
                $(PORT_BASE)somd$(SEP)om.idl &
                $(PORT_BASE)somd$(SEP)orb.idl &
                $(PORT_BASE)somd$(SEP)poa.idl &
                $(PORT_BASE)somd$(SEP)principl.idl &
                $(PORT_BASE)somd$(SEP)request.idl &
                $(PORT_BASE)somd$(SEP)rhborbst.idl &
                $(PORT_BASE)somd$(SEP)rhbtypes.idl &
                $(PORT_BASE)somd$(SEP)servmgr.idl &
                $(PORT_BASE)somd$(SEP)servreq.idl &
                $(PORT_BASE)somd$(SEP)somdcprx.idl &
                $(PORT_BASE)somd$(SEP)somdfenc.idl &
                $(PORT_BASE)somd$(SEP)somdmprx.idl &
                $(PORT_BASE)somd$(SEP)somdobj.idl &
                $(PORT_BASE)somd$(SEP)somdom.idl &
                $(PORT_BASE)somd$(SEP)somdserv.idl &
                $(PORT_BASE)somd$(SEP)somdstub.idl &
                $(PORT_BASE)somd$(SEP)somdtcdt.idl &
                $(PORT_BASE)somd$(SEP)somdtype.idl &
                $(PORT_BASE)somd$(SEP)somoa.idl &
                $(PORT_BASE)somd$(SEP)stexcep.idl &
                $(PORT_BASE)somd$(SEP)unotypes.idl &
                $(PORT_BASE)somem$(SEP)clientev.idl &
                $(PORT_BASE)somem$(SEP)eman.idl &
                $(PORT_BASE)somem$(SEP)emregdat.idl &
                $(PORT_BASE)somem$(SEP)event.idl &
                $(PORT_BASE)somem$(SEP)sinkev.idl &
                $(PORT_BASE)somem$(SEP)timerev.idl &
                $(PORT_BASE)somem$(SEP)workprev.idl &
                $(PORT_BASE)somp$(SEP)defedidl.idl &
                $(PORT_BASE)somp$(SEP)eda.idl &
                $(PORT_BASE)somp$(SEP)fma.idl &
                $(PORT_BASE)somp$(SEP)fmi.idl &
                $(PORT_BASE)somp$(SEP)fmib.idl &
                $(PORT_BASE)somp$(SEP)fsagm.idl &
                $(PORT_BASE)somp$(SEP)fsgm.idl &
                $(PORT_BASE)somp$(SEP)iogma.idl &
                $(PORT_BASE)somp$(SEP)iogrp.idl &
                $(PORT_BASE)somp$(SEP)mia.idl &
                $(PORT_BASE)somp$(SEP)nsma.idl &
                $(PORT_BASE)somp$(SEP)objset.idl &
                $(PORT_BASE)somp$(SEP)pid.idl &
                $(PORT_BASE)somp$(SEP)po.idl &
                $(PORT_BASE)somp$(SEP)poid.idl &
                $(PORT_BASE)somp$(SEP)poida.idl &
                $(PORT_BASE)somp$(SEP)psma.idl &
                $(PORT_BASE)somp$(SEP)somp.idl &
                $(PORT_BASE)somp$(SEP)somperr.idl &
                $(PORT_BASE)somp$(SEP)somperrd.idl &
                $(PORT_BASE)somp$(SEP)sompstad.idl &
                $(PORT_BASE)somp$(SEP)sompstat.idl &
                $(PORT_BASE)somp$(SEP)srkset.idl &
                $(PORT_BASE)somu$(SEP)snglicls.idl &
                $(PORT_BASE)somu$(SEP)sombacls.idl &
                $(PORT_BASE)somu$(SEP)somida.idl &
                $(PORT_BASE)somu$(SEP)somproxy.idl &
                $(PORT_BASE)somu$(SEP)somsid.idl &
                $(PORT_BASE)some$(SEP)scattrib.idl &
                $(PORT_BASE)some$(SEP)scbase.idl &
                $(PORT_BASE)some$(SEP)scclass.idl &
                $(PORT_BASE)some$(SEP)sccommon.idl &
                $(PORT_BASE)some$(SEP)scconst.idl &
                $(PORT_BASE)some$(SEP)scdata.idl &
                $(PORT_BASE)some$(SEP)scemit.idl &
                $(PORT_BASE)some$(SEP)scentry.idl &
                $(PORT_BASE)some$(SEP)scenum.idl &
                $(PORT_BASE)some$(SEP)scenumnm.idl &
                $(PORT_BASE)some$(SEP)scmeta.idl &
                $(PORT_BASE)some$(SEP)scmethod.idl &
                $(PORT_BASE)some$(SEP)scmodule.idl &
                $(PORT_BASE)some$(SEP)scparm.idl &
                $(PORT_BASE)some$(SEP)scpass.idl &
                $(PORT_BASE)some$(SEP)scseqnce.idl &
                $(PORT_BASE)some$(SEP)scstring.idl &
                $(PORT_BASE)some$(SEP)scstruct.idl &
                $(PORT_BASE)some$(SEP)sctdef.idl &
                $(PORT_BASE)some$(SEP)sctmplt.idl &
                $(PORT_BASE)some$(SEP)scunion.idl &
                $(PORT_BASE)some$(SEP)scusrtyp.idl &
                $(PORT_BASE)some$(SEP)somstrt.idl &
                $(PORT_BASE)soms$(SEP)somssock.idl &
                $(PORT_BASE)somst$(SEP)tcpsock.idl &
                $(PORT_BASE)somestrm$(SEP)omgestio.idl &
                $(PORT_BASE)somestrm$(SEP)somestio.idl &
                $(PORT_BASE)somnmf$(SEP)biter.idl &
                $(PORT_BASE)somnmf$(SEP)lname.idl &
                $(PORT_BASE)somnmf$(SEP)lnamec.idl &
                $(PORT_BASE)somnmf$(SEP)naming.idl &
                $(PORT_BASE)somnmf$(SEP)xnaming.idl &
                $(PORT_BASE)somnmf$(SEP)xnamingf.idl &
                $(PORT_BASE)somabs1$(SEP)omgidobj.idl &
                $(PORT_BASE)somcorba$(SEP)corba.idl &
                $(PORT_BASE)somref$(SEP)somref.idl &
                $(PORT_BASE)somos$(SEP)somap.idl &
                $(PORT_BASE)somos$(SEP)somos.idl &
                $(PORT_BASE)somcslib$(SEP)xmscssae.idl &
                $(PORT_BASE)somu2$(SEP)somtdm.idl

$(TARGETS): $(SOURCES)
 $(verbose)pdl.exe -o $(PATH) $< #$(BLACKHOLE)
